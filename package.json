{
  "name": "recurly-apple-observe-error",
  "version": "1.0.0",
  "scripts": {
    "start": "npm i && git add . && git commit -m \"update\" && git push && npm run build",
    "build": "npx webpack --config webpack.config.js -p"
  },
  "dependencies": {
    "promise": "^8.1.0",
    "uglifyjs-webpack-plugin": "^2.2.0",
    "webpack": "^5.72.0",
    "webpack-cli": "^4.9.2"
  }
}
